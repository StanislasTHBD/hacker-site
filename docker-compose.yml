version: '3.8'

services:
  # ======================================
  # 🗂️ 1️⃣ Ton site Next.js
  app:
    build: ./
    container_name: dataleak-app
    expose:
      - "3000"          # port interne
    ports:
      - "3000:3000"     # port externe → http://localhost:3000
    restart: always
    depends_on:
      - tor             # attend que Tor démarre

  # ======================================
  # 🧅 2️⃣ Ton service Tor proxy
  tor:
    image: dperson/torproxy
    container_name: dataleak-tor
    restart: always
    ports:
      - "9050:9050"   # port SOCKS
    volumes:
      - ./tor/torrc:/etc/tor/torrc:ro   # Fichier config Tor
      - tor-data:/var/lib/tor           # Persistance clé Tor
    depends_on:
      - app

volumes:
  # ======================================
  # 📦 Volume persistant pour Tor
  tor-data:
